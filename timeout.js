function printHello() {
    console.log("Hello world!");
}

// Display after some time
// var t = setTimeout(printHello, 2000);

// Dont display after some time
// clearTimeout(t);

// Print every so often
setInterval(printHello, 2000);